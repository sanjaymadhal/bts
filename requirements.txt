fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.11.7
pydantic-settings==2.6.0
supabase==2.31.0
httpx==0.27.2
python-jose[cryptography]==3.3.0
cachetools==5.5.0
email-validator==2.2.0
bcrypt==4.2.0
paho-mqtt==1.6.1
# Expo push: uses the existing httpx client to POST directly to
# https://exp.host/--/api/v2/push/send. The official expo-server-sdk
# isn't published to PyPI; hand-rolling keeps the dependency surface
# small and lets us monkey-patch the sender in tests.
pytest==8.3.3
pytest-asyncio==0.24.0
gunicorn